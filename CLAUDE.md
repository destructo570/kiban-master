See @.claude/design.md for UI design guidelines — follow these whenever working on UI/frontend changes.

See @.claude/ai.md for AI SDK conventions — follow these whenever touching `apps/backend/src/lib/ai/` or any route that talks to a model.
